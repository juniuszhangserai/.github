name: 'Junius Test Action'
description: 'Junius Test Action'
inputs:
  username:
    description: 'username'
    required: true
    default: 'Junius'
runs:
  using: "composite"
  steps:
    - run: echo Hello $INPUT_USERNAME
      shell: bash