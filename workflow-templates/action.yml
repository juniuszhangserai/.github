name: 'Junius Test Action'
description: 'Junius Test Action'
inputs:
  username:
    description: 'username'
    required: true
    default: 'Junius'
runs:
  using: "composite"
  steps:
    - run: echo Hello with $INPUT_USERNAME
      shell: bash